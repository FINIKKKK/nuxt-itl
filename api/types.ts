export type TBase = {
    id: number;
    created_at: string;
    updated_at: string;
};

export type TData = {
    status: number;
};

export type TMessage = {
    status: number;
    message: string;
};

